// The bell's place in the top bar (Piece 29).
//
// It shows nothing at all yet. Piece 30 builds the notifications themselves —
// the three triggers, the unread count and the panel — and fills this in. It
// exists now so the bar's layout is the finished one and Piece 30 changes this
// file only.

export default function NotificationBell() {
  return null;
}
